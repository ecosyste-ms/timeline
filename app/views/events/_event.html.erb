<div class="card mb-3 event" id="event_<%= event.id %>">
  <div class="card-body pb-1">
    <%= link_to "https://github.com/#{event.actor.gsub('[bot]', '')}", target: :_blank, class: 'float-end ms-2 mb-5' do %>
      <img src="https://github.com/<%= event.actor.gsub('[bot]', '') %>.png" class="rounded" height='40' width='40' onerror="this.style.display='none'">
    <% end %>
    <h5 class="card-title">
      <span class='me-2 mb-5 float-start' title='<%= event_name(event.event_type) %>'>
        <%= octicon(event_icon(event), height: 18) %>
      </span>

      <%= link_to event.actor, "https://github.com/#{event.actor.gsub('[bot]', '')}", target: :_blank %>

      <%= event.action_text %>

      <%= link_to event.repository, "https://github.com/#{event.repository}", target: :_blank %>
      
      <small class='text-muted' title="<%= event.created_at %>">
        <%= distance_of_time_in_words_to_now event.created_at %> ago
      </small>
    </h5>
    <div class="ms-4">
      <%= render partial: "events/#{event.event_type.underscore}", locals: {event: event}  %>
    </div>
  </div>
</div>
